/* export const useCharacterStore = () => {
  const characterList = useState("characterList", () => []);

  const updateCharacterList = (newList) => {
    characterList.value = newList;
  };

  return {
    characterList,
    updateCharacterList,
  };
};
 */
